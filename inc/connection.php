<?php

    $dbhost = "localhost";
    $dbuser = "root";
    $dbpassword = "";
    $dbname = "prime_db";

    $conn = new mysqli($dbhost, $dbuser, $dbpassword, $dbname) or die("Connect failed: %s\n" . $connect->connect);

    ?>
